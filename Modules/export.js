//export const bankName = "HDFC Bank";

export function getEmployeeDetails(EmployeeName, Position){
console.log(`${EmployeeName} works as ${Position} in MNC company`)
    
}

var multiply = (a,b) => a*b;
var divide = (a,b) => a/b;
export {multiply, divide};

export function bankDetails(bankname, bankemployee){
    console.log(`${bankemployee} works in ${bankname}`)
}